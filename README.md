# Megadriod-NetRecon

downlaod the folder 
open the vscode
install python form the extension
open the folder in the vscode
open the terminal and run pip install -r requirements.txt
and run sfter the pip install  run the uvicorn main:app --reload

make you you install the npcap to get live cativity from your computer
